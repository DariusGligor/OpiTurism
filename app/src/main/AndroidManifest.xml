<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.opiniaturistului">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcheralabun_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcheralabun_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpiniaTuristului">
        <activity android:name=".v23"></activity>
        <activity android:name=".lista" />
        <activity android:name=".v22" />
        <activity android:name=".cauta" />
        <activity android:name=".adaugaa" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>